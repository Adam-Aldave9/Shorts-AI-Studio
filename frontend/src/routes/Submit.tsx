// Submit screen (spec §9.2): premise, duration, style, voice. POST /briefs.
// Stub — wire up in Week 3.
export default function Submit() {
  return <h1 className="text-xl font-semibold">Submit a brief</h1>;
}

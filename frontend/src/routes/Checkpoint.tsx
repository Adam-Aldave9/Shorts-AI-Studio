// Checkpoint screen (spec §4.3, §9.2): two-pane summary + Monaco JSON editor.
// The one screen that cannot be cut — the human-in-the-loop gate. Stub.
export default function Checkpoint() {
  return <h1 className="text-xl font-semibold">Checkpoint</h1>;
}

// Status screen (spec §9.2): live DAG view, node pills, queue depth, cost-to-date.
// SSE-driven (GET /packages/{id}/events). Stub.
export default function Status() {
  return <h1 className="text-xl font-semibold">Execution status</h1>;
}

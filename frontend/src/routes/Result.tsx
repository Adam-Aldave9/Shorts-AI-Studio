// Result screen (spec §9.2): embedded MP4 player, download, per-node cost,
// wall-clock vs critical path. Stub.
export default function Result() {
  return <h1 className="text-xl font-semibold">Result</h1>;
}
